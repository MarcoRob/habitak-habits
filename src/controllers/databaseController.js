'use strict';
import mongoose from 'mongoose';
var Habit = mongoose.model('Habits');

export let findObject = (queryObject) => {

}

export let removeObject = (queryObject) => {

}

export let updateObject = (queryObject) => {
    
}